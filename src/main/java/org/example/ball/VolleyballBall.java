package org.example.ball;

import org.example.ball.enums.Color;
import org.example.ball.enums.Material;

public class VolleyballBall extends Ball {
    public VolleyballBall(int diameter, Color color, Material material) {
        super(diameter, color, material);
    }
}
